name: mediqueue_mobile
description: "MediQueue — Emergency dispatch and appointment booking."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  supabase_flutter: ^2.5.0
  go_router: ^13.0.0
  flutter_riverpod: ^2.5.0
  geolocator: ^12.0.0
  http: ^1.2.0
  shared_preferences: ^2.2.0
  flutter_animate: ^4.5.0
  google_fonts: ^6.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
